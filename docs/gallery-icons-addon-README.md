# Sri Sashti addon bundle

This bundle contains ready-to-use WebP gallery images, card crops, SVG UI icons, PWA icons, an asset map, and a VS Code Codex implementation prompt.

Use `CODEX-PROMPT.md` in VS Code Codex after copying/attaching this bundle.

Do not use `docs/reference-icon-gallery-layout.png` as a treatment image; it is only a visual reference.
